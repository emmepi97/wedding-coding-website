# Wedding Coding — Headless Static

Branch prepared from the current repository.

This branch intentionally contains only the static frontend currently used by GitHub Pages.

- `docs/` is preserved as supplied.
- Django/backend files have been removed from this branch.
- No redesign or refactoring has been applied to the individual frontend files.
- The production `main` branch remains untouched.

## Next step

Create a Git branch such as `refactor/headless`, upload this folder to that branch, and verify the site before merging into `main`.
