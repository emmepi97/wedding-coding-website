# Wedding Coding — Static Frontend

This repository contains only the static frontend currently published with GitHub Pages.

All active website files are contained in `docs/`.

GitHub Pages configuration:
- Branch: `main`
- Folder: `/docs`

The individual frontend pages have been preserved without redesign or functional changes.
